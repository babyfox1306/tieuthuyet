{
  "running": false,
  "phase": "write",
  "write_mode": "supervised",
  "ok": true,
  "written": 0,
  "failed_ch": null,
  "issue_chapters": [
    7,
    17,
    18,
    19,
    20
  ],
  "skipped_chapters": [],
  "error": "",
  "log": [
    "Viet chuong 7–22 (giam sat — loi de lai, chay het)",
    "▶ ch7 dang viet... (write #1; content cap 2 ben trong writer)",
    "⚠ ch7: needs_fix — [format_fix — chỉ sửa tay, không rewrite]; markdown advisory (5 hit) — dùng UI fixer, không chặn promote; khoảng trắng thừa cuối dòng (stray whitespace) [content_retries=0/2]",
    "  → [format_fix — chỉ sửa tay, không rewrite]",
    "  → markdown advisory (5 hit) — dùng UI fixer, không chặn promote",
    "  → khoảng trắng thừa cuối dòng (stray whitespace)",
    "⏸ ch7: chỉ lỗi format (vd. *) → needs_fix — user sửa tay, AUTO không rewrite",
    "⊘ ch8: da co (catalog) — bo qua",
    "⊘ ch9: da co (catalog) — bo qua",
    "⊘ ch10: da co (catalog) — bo qua",
    "⊘ ch11: da co (catalog) — bo qua",
    "⊘ ch12: da co (catalog) — bo qua",
    "⊘ ch13: da co (catalog) — bo qua",
    "⊘ ch14: da co (catalog) — bo qua",
    "⊘ ch15: da co (catalog) — bo qua",
    "⊘ ch16: da co (catalog) — bo qua",
    "▶ ch17 dang viet... (write #1; content cap 2 ben trong writer)",
    "⚠ ch17: needs_fix — [format_fix — chỉ sửa tay, không rewrite]; markdown advisory (2 hit) — dùng UI fixer, không chặn promote; khoảng trắng thừa cuối dòng (stray whitespace) [content_retries=0/2]",
    "  → [format_fix — chỉ sửa tay, không rewrite]",
    "  → markdown advisory (2 hit) — dùng UI fixer, không chặn promote",
    "  → khoảng trắng thừa cuối dòng (stray whitespace)",
    "⏸ ch17: chỉ lỗi format (vd. *) → needs_fix — user sửa tay, AUTO không rewrite",
    "▶ ch18 dang viet... (write #1; content cap 2 ben trong writer)",
    "⚠ ch18: needs_fix — [format_fix — chỉ sửa tay, không rewrite]; markdown advisory (2 hit) — dùng UI fixer, không chặn promote; khoảng trắng thừa cuối dòng (stray whitespace) [content_retries=0/2]",
    "  → [format_fix — chỉ sửa tay, không rewrite]",
    "  → markdown advisory (2 hit) — dùng UI fixer, không chặn promote",
    "  → khoảng trắng thừa cuối dòng (stray whitespace)",
    "⏸ ch18: chỉ lỗi format (vd. *) → needs_fix — user sửa tay, AUTO không rewrite",
    "▶ ch19 dang viet... (write #1; content cap 2 ben trong writer)",
    "⚠ ch19: needs_fix — [format_fix — chỉ sửa tay, không rewrite]; khoảng trắng thừa cuối dòng (stray whitespace) [content_retries=0/2]",
    "  → [format_fix — chỉ sửa tay, không rewrite]",
    "  → khoảng trắng thừa cuối dòng (stray whitespace)",
    "⏸ ch19: chỉ lỗi format (vd. *) → needs_fix — user sửa tay, AUTO không rewrite",
    "▶ ch20 dang viet... (write #1; content cap 2 ben trong writer)",
    "⚠ ch20: needs_fix — [format_fix — chỉ sửa tay, không rewrite]; khoảng trắng thừa cuối dòng (stray whitespace) [content_retries=0/2]",
    "  → [format_fix — chỉ sửa tay, không rewrite]",
    "  → khoảng trắng thừa cuối dòng (stray whitespace)",
    "⏸ ch20: chỉ lỗi format (vd. *) → needs_fix — user sửa tay, AUTO không rewrite",
    "⊘ ch21: da co (catalog) — bo qua",
    "▶ ch22 dang viet... (write #1; content cap 2 ben trong writer)",
    "⊘ ch22: da ready — bo qua",
    "--- Ket thuc batch: 0 ch da viet; 10 ready (bo qua); 5 can sua: ch7, ch17, ch18, ch19, ch20"
  ],
  "updated_at": "2026-07-20T03:08:28.168450+00:00"
}